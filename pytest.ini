[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = Test*

markers =
    real_api: marks tests that require real API access (deselect with '-m "not real_api"')

addopts = -v --tb=short -m "not real_api"

filterwarnings =
    ignore::DeprecationWarning
